<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>投稿の編集</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-5">

  <h1 class="mb-4">投稿の編集</h1>

  <form action="/posts/<%= post.id %>/update" method="POST" enctype="multipart/form-data">
    <div class="mb-3">
      <label class="form-label">タイトル</label>
      <input type="text" name="title" class="form-control" value="<%= post.title %>" required>
    </div>

    <div class="mb-3">
      <label class="form-label">内容</label>
      <textarea name="content" class="form-control" rows="5" required><%= post.content %></textarea>
    </div>

    <% if (post.filePath) { %>
      <div class="mb-3">
        <label class="form-label">現在のファイル</label><br>
        <% if (post.filePath.endsWith('.pdf')) { %>
          <a href="<%= post.filePath %>" target="_blank" class="btn btn-outline-secondary">PDF を開く</a>
        <% } else { %>
          <img src="<%= post.filePath %>" alt="添付画像" style="max-width: 200px;" class="img-thumbnail">
        <% } %>
      </div>
    <% } %>

    <div class="mb-3">
      <label class="form-label">ファイルの差し替え</label>
      <input type="file" name="file" class="form-control">
    </div>

    <button type="submit" class="btn btn-primary">更新</button>
    <a href="/" class="btn btn-secondary">キャンセル</a>
  </form>

</body>
</html>
